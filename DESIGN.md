# RouteCanvas 设计系统（DESIGN.md）

> 本文件是 RouteCanvas 的**品牌设计契约**。任何 AI（IDE Agent / MCP / 应用内助手）在生成或修改画布时，**必须遵守**本文件的约束。
> 运行时精简版见 `src/data/designSystem.ts`，两者需保持一致。

---

## 1. 设计原则

1. **结构优先**：生成的是结构化节点（JSON），不是图片。每个组件必须是组件白名单中的合法 `type`。
2. **克制**：一个页面聚焦一个核心目标，不堆砌组件。留白与内容同等重要。
3. **一致**：同类元素使用相同的颜色、字号、间距、圆角。
4. **层级清晰**：通过字号、字重、颜色对比建立视觉层级，主操作永远最突出。

---

## 2. 颜色令牌（Color Tokens）

| 用途 | 色值 | 说明 |
|------|------|------|
| 主色 primary | `#6366f1` | 主按钮、关键链接、选中态 |
| 主色-深 | `#4f46e5` | hover / 强调 |
| 辅助色 accent | `#ec4899` | 次级强调、徽章、渐变端点 |
| 成功 success | `#22c55e` | 成功状态、正向指标 |
| 警告 warning | `#f59e0b` | 警告状态 |
| 危险 danger | `#ef4444` | 删除、错误、负向指标 |
| 正文 text | `#1f2937` | 标题与正文 |
| 次级文本 muted | `#6b7280` | 说明文字、占位符 |
| 背景 surface | `#ffffff` | 卡片、输入框底色 |
| 画布背景 canvas | `#f9fafb` | 页面画板底色 |
| 边框 border | `#e5e7eb` | 分割线、输入框描边 |

**渐变约定**：品牌渐变统一为 `#6366f1 → #ec4899`（左上到右下）。

---

## 3. 字体排印（Typography）

| 层级 | 字号 | 字重 | 用途 |
|------|------|------|------|
| 大标题 H1 | 28–32 | 700 | 页面主标题（每页最多 1 个） |
| 标题 H2 | 20–22 | 600 | 区块标题 |
| 小标题 H3 | 16 | 600 | 卡片/分组标题 |
| 正文 body | 14 | 400 | 主要内容 |
| 辅助 caption | 12 | 400 | 说明、时间戳 |
| 按钮文字 | 14 | 500 | 按钮、链接 |

**行高**：正文 1.5–1.6，标题 1.2–1.3。
**单行宽度**：正文文本不超过 60 个字符（约 480px），超长需换行或截断。

---

## 4. 间距与布局（Spacing & Layout）

- **间距基数**：8px。所有间距取 8 的倍数（8 / 16 / 24 / 32 / 48）。
- **页面画板**：默认 800 × 600，可按内容扩展，但宽度建议保持 800。
- **页内边距**：内容距画板边缘至少 40px。
- **组件间距**：
  - 相关元素组内：8–16px
  - 区块之间：24–32px
  - 大章节之间：48px
- **对齐**：同一区块内的组件左对齐（或居中对齐），禁止随意错位。
- **圆角**：
  - 小组件（按钮/输入框/徽章）：8px
  - 卡片/容器：12–16px
  - 头像/图标容器：50%（圆形）
- **阴影**：卡片使用柔和阴影，避免多重阴影叠加。

---

## 5. 组件使用规范

| 场景 | 推荐组件 | 约束 |
|------|----------|------|
| 主要操作（每页 ≤1 个） | `Button`（variant=primary） | 用主色，放在视觉终点 |
| 次要操作 | `Button`（variant=secondary/ghost） | 不与主操作抢焦点 |
| 跳转链接 | `Link` | 用主色，带下划线或 hover 态 |
| 表单输入 | `Input` / `Textarea` / `Select` | 必须有标签或占位符 |
| 信息分组 | `Card` / `Container` | 一组相关内容放一个卡片 |
| 状态提示 | `Alert` / `Badge` / `Notification` | 用语义色（成功/警告/危险） |
| 数据展示 | `Table` / `List` / `StatCard` | 数字右对齐，带单位 |
| 页面骨架 | `Navbar`（顶部）+ `Footer`（底部） | 导航页必须有 Navbar |
| 大区块 | `Section` / `AnimSection` | 用于落地页分屏 |

**禁止事项**：
- 不要为纯文本创建 `Button`（应使用 `Text`）。
- 不要在一个页面放超过 2 个 primary 按钮。
- 不要使用白名单之外的组件 type。
- 不要生成重叠遮挡的组件（除非明确需要浮层）。

---

## 6. 页面模式（Mode）

- **navigate**（默认）：点击跳转到独立页面。用于多页面流程（登录→首页→详情）。
- **scroll**：滚动续页。用于单页落地页（hero→功能→定价→footer）。
  - scroll 模式下，多个 `Section` 垂直堆叠在同一页面，间距 0，形成连续滚动。

---

## 7. 命名与文案

- 页面 `name` 用中文功能名（如「登录页」「商品详情」），`path` 用英文小写短横线（如 `/login`、`/product-detail`）。
- 组件文案简洁口语化，按钮用动词（「登录」「立即购买」「了解更多」）。
- 占位数据用真实感示例（「张三」「¥199」），不用 lorem ipsum。

---

## 8. AI 自检清单（生成后自查）

- [ ] 所有组件 type 都在白名单内？
- [ ] 每页只有 1 个主标题、1 个 primary 按钮？
- [ ] 间距是 8 的倍数、组件对齐？
- [ ] 颜色只用了本文件定义的令牌？
- [ ] 页面 name/path 符合命名规范？
- [ ] 连线（transition）的 source/target 都存在？
