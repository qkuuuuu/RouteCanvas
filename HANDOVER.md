# RouteCanvas 任务交接书

> 最后更新：2026-08-06  
> 当前版本：V1.2 开发完成  
> 产品定位：AI-first 前端设计平台，核心模型为“项目 > 页面 > 页面间流程”。无限画布负责页面设计与排布，流程视图负责检查和维护交互关系。

## 一、当前验证状态

以下命令已在 Windows / PowerShell 环境通过：

```bash
npm run build
npx tsc --noEmit
npm run lint
npm test
cd mcp && npx tsc --noEmit
```

2026-08-06 生产构建结果：

- Next.js 14.2.35 生产构建成功。
- 首页路由由约 425 kB 降至 174 kB，First Load JS 由约 513 kB 降至 502 kB。
- ESLint 0 warning / 0 error，主项目与 MCP TypeScript 0 error，Vitest 核心用例全部通过。
- Webpack 仅报告本机缓存快照失败警告，不影响生产产物。

## 二、P0 / P1 缺陷关闭情况

| 问题 | 状态 | 实现说明 |
|---|---|---|
| P0：生产构建失败 | 已关闭 | 干净删除 `.next` 后重新构建通过；旧 `/api/ai` 路由已移除，避免重复入口与陈旧路由类型。 |
| P0：HANDOVER 与实际代码不一致 | 已关闭 | 本文已按当前代码、真实数量和验证结果重写。 |
| P1：AI 新页面语义有漏洞 | 已关闭 | `forceNew` 区分“新建页/再加一页”和“复用空白页”；明确新增时不会误复用旧空白页。 |
| P1：版本回退不完整 | 已关闭 | 快照保存页面名、layout、原节点 ID、完整节点结构和出向 transitions；持久化到 localStorage；属性面板可查看并恢复历史版本。 |
| P1：流程图生成虚假触发节点 | 已关闭 | 空页面禁止创建伪 `page:<id>` 触发节点，要求先选择真实按钮/组件。 |
| P1：连线去重粒度过粗 | 已关闭 | 去重包含 source、target、event、mode 和 params；前端 Store 与 MCP 同步。 |
| P1：原型预览与响应式画布不一致 | 已关闭 | Desktop/Mobile 统一使用 `resolvePageFrames()`；长页面使用真实滚动视口。 |
| P1：导出交互不完整 | 已关闭 | HTML、React、Vue、Compose 均映射点击交互；导航与滚动区域使用统一语义；Image/Text 也支持交互。 |
| P1：AI 操作提案缺少依赖关系 | 已关闭 | 选择子操作自动带上前置依赖，取消父操作会取消下游引用；整批执行失败自动回滚。 |
| P1：AI 状态文案不一致 | 已关闭 | 全局 AI Agent 为唯一配置入口，聊天框只显示真实连接状态；未配置时提前阻断并给出一致提示。 |

## 三、已实现产品能力

### 1. AI 设计链路

- 全局 AI Agent 配置：API Key、Model、Base URL、连接测试，仅保存在当前浏览器。
- AI 设计会话负责生成、修改、审美选择、计划审核与执行；旧 Toolbar `AI Prompt` 和旧 `/api/ai` 已删除。
- 13 个技能包：PPT、Web、Mobile、Dashboard、视觉图像、邮件、线框、HTML、React、Vue、Compose、图转 3D、通用工具。
- 12 套审美主题继续保留；AI 允许基于设计意图突破单个 token。
- 多页 PPT 逐页调用、逐页落盘；中断或任一页失败时回滚整个生成事务，不保留半成品。
- 修改提案支持逐项勾选、全部应用、选中应用、取消本次；操作显示依赖关系并在画布高亮受影响节点。
- Base URL 只允许 HTTP/HTTPS，默认阻止 localhost、私网地址和 `.local`；本地模型需显式设置 `ROUTECANVAS_ALLOW_LOCAL_AI=true`。

### 2. 无限画布与页面流程

- 无限画布同时展示项目内所有页面，顶部固定“+ 页面”入口，显示页面名、尺寸、位置和页码。
- 白色画板空白处拖动页面；Space + 拖动或中键拖动平移无限画布；两种行为在快捷键帮助中区分。
- 普通滚轮以鼠标位置为中心连续缩放；Shift + 滚轮平移。
- 图片和普通节点拖动使用本地预览，松手一次提交；取消原生图片拖拽，使用 GPU transform，避免大图拖动卡顿和松手回跳。
- 页面设计与交互流程共享同一份 pages/transitions 数据，不是两个独立系统。
- 选中节点后，右侧直接设置“无动作 / 前往页面 / 滚动到区域”；流程图作为高级检查与批量维护视图。
- 原型演示支持响应式设备切换、页面导航、当前页区域滚动和登录守卫提示。

### 3. 编辑体验

- 8 向缩放手柄、Shift 等比、双击文本编辑、方向键微移、Alt 拖拽复制、Ctrl+D、Delete。
- 对齐参考线、多选、批量移动/删除、六向对齐和横纵分布。
- Stack/Grid Auto Layout 支持 gap、padding、align、justify、列数和嵌套 parentId。
- 主组件修改后自动检测，询问用户“采纳并同步 / 保持实例不变”；同步属性、尺寸和响应式覆盖。
- 评论钉、区域截图标注、将评论交给 AI 修改。
- 版本历史持久化，在页面属性中展示时间、节点数、交互数，并可恢复指定版本；恢复前会自动再保存当前状态。

### 4. 组件库

- 独立滚动容器，分类和二级分类均显示数量。
- 搜索结果按精确匹配、前缀匹配、包含匹配排序。
- 本地持久化收藏和最近使用。
- 导入图片添加到当前活动页面，而不是错误地落到第一个页面。

### 5. 交付

- JSON 导入/导出、PNG、PDF、单文件演示 HTML。
- HTML、React、Vue 3 SFC、Jetpack Compose 代码导出。
- 响应式 frame、点击导航、滚动区域语义在画布、原型与各导出端统一。
- MCP Server 保留，`@xyflow/react` 继续用于流程视图，不能整体移除。

## 四、已确认的产品决策

1. 保留 12 套主题，参考 Open Design 的设计完成度继续升级，不把 token 当硬约束。
2. 审美预览优先使用轻量实时微缩页面；生成后以真实画布为最终预览。
3. 多页生成允许逐页出现，但失败不保留半成品。
4. 修改审核同时提供全部、选中、取消；Diff 优先采用画布内高亮 + 文字影响清单。
5. Auto Layout 先完成实用的 Stack/Grid，并把容器 parentId 映射为真实结构层级。
6. 主组件改动自动检测，但同步实例前让用户确认。
7. V1.2 投入 PPTX、分享链接和自动化编排。
8. MVP 明确排除实时多人协同。

## 五、V1.2 完成情况与边界

V1.2 计划项已经落地：

- PPTX 文件导出：在“更多项目操作”中直接导出 `.pptx`，每个项目页面对应一张幻灯片，附带页面名称与路由备注。浏览器实测已生成可识别的 PowerPoint 文件。
- 分享链接：创建后端持久化只读快照和短链接 `/share/<id>`，分享页使用统一原型播放器，不允许编辑源项目。
- 自动化编排：支持手动、新页面创建后、AI 修改采纳后三类触发器，以及动作节点、失败停止、重试、运行日志和本地持久化。
- 12 套审美主题均已升级为 mood、palette、typography、composition、components、imagery、motion、antiPatterns、finishing 九段式方法论，并注入 AI 规划提示。
- AI Diff 支持前后快照对比、影响清单、画布高亮和整批撤销；画布在应用后被继续修改时会阻止不安全撤销。
- 多选节点支持整体八向缩放；图层面板支持拖拽排序并归一化 zIndex。
- Stack/Grid 容器和 `parentId` 已映射为 HTML、React、Vue 的嵌套 DOM，以及 Compose 的嵌套 Box/flex/grid 语义。
- 3D、动效和组件包改为首次使用时动态加载，首页路由体积显著下降。
- 已建立 Vitest 基础测试，覆盖 AI 操作依赖、页面 transition 完整性、Stack/嵌套导出和审美方法论。

当前明确边界：

- 分享快照由当前部署服务器的文件存储承载，尚未接入账号、权限控制和外部对象存储；不应表述为完整云协作。
- PPTX 以页面高清截图写入幻灯片，保证视觉一致，但页面元素不是可单独编辑的 PowerPoint 图形。
- 自动化中的“交给 AI 执行”会派发到 AI 会话并等待用户审核，不是后台无人值守执行。
- 图层拖拽当前调整同级/全局 zIndex，不会通过拖拽改变 `parentId`；多人实时协同继续排除在 MVP 外。

## 六、快速验证路径

1. 首页新建空白项目，确认右侧画布展开；点“+ 页面”确认新页面出现在同一无限画布。
2. 在画布内滚轮缩放，确认缩放中心跟随鼠标；Space + 拖动确认画布平移。
3. 从组件库导入图片，确认进入当前页面；拖动时连续移动，松手后位置保持。
4. 选中按钮，在属性面板分别测试“前往页面”和“滚动到区域”，再进入原型演示。
5. 进入交互流程，空页面连接应提示先选触发组件，不应生成虚假节点。
6. 让 AI 修改画布，逐项勾选提案；制造失败时确认整批回滚。
7. 重做页面后，在页面属性的版本历史中恢复上一版。
8. 在“更多项目操作”中导出 PPTX，确认下载文件可由 PowerPoint 打开；创建分享链接并确认 `/share/<id>` 为只读预览。
9. 新建自动化，分别验证手动运行、新页面创建后和 AI 修改采纳后的触发与运行日志。
10. 执行 `npm run build`、`npx tsc --noEmit`、`npm run lint`、`npm test` 和 `cd mcp && npx tsc --noEmit`。
