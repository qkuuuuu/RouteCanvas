/**
 * 设计系统运行时精简版（DESIGN.md 的 prompt 版本）
 * 注入到应用内 AI（Chat / 标注层）的 system prompt，约束生成风格。
 * 注意：与根目录 DESIGN.md 保持一致，修改时需同步。
 */
export const DESIGN_RULES = `## 设计系统约束（必须遵守）
颜色令牌：主色 #6366f1、主色深 #4f46e5、辅助 #ec4899、成功 #22c55e、警告 #f59e0b、危险 #ef4444、正文 #1f2937、次级文本 #6b7280、背景 #ffffff、边框 #e5e7eb。品牌渐变 #6366f1→#ec4899。
字体：H1 28-32/700（每页最多1个）、H2 20-22/600、H3 16/600、正文 14/400、辅助 12/400、按钮 14/500。
间距：基数 8px（只用 8/16/24/32/48）。页内边距≥40px。相关元素 8-16px，区块间 24-32px，章节间 48px。
圆角：按钮/输入框/徽章 8px，卡片/容器 12-16px。
布局：同区块组件左对齐或居中对齐，禁止随意错位与重叠。桌面页 800×600，移动端页 390×844。
组件规范：每页≤1个 primary 按钮且放视觉终点；纯文本用 Text 不用 Button；表单输入必须有标签/占位符；状态提示用语义色。
命名：页面 name 用中文功能名，path 用英文小写短横线；按钮文案用动词；占位数据用真实感示例。
生成后自检：组件 type 在白名单内？每页只有1个主标题和1个 primary 按钮？间距是8的倍数且对齐？颜色只用令牌色？`;
