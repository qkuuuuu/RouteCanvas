# 留空则使用纯前端 localStorage 模式（默认）
NEXT_PUBLIC_CLOUD_API=

# 启用可选后端时设为 /api
# NEXT_PUBLIC_CLOUD_API=/api

# OpenAI API Key（可选）
# 方式一：部署方在此预设（生产环境推荐）
# 方式二：用户在前端 AI 面板的「设置」中输入，存浏览器 localStorage
# 两种方式可共存，前端用户 key 优先级更高
OPENAI_API_KEY=
